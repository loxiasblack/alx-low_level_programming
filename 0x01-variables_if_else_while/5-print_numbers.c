#include<stdio.h>
/**
 * main - Entry point
 * Description: 'print all single digit base on 10'
 * Return: Always 0 (Success)
 */
int main(void)
{
int i = 0;

for (i = 0 ; i < 10 ; i++)
{
	printf("%d", i);
}
printf("\n");
return (0);
}
