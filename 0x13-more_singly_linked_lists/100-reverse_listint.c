#include <stdio.h>
#include <stdlib.h>
#include "lists.h"

listint_t *reverse_listint(listint_t **head)
{
    listint_t *prev_node, *next_node;

    while (*head)
    {
        prev_node = *head;
        next_node->next =
    }
    
    
}