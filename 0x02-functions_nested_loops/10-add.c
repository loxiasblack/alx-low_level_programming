#include "main.h"
/**
 * add - Entry point
 * Description: 'add to numbers'
 * @a: first integer
 * @b: second integer
 * Return: Always results.
 */
int add(int a, int b)
{
	int results;

	results = a + b;
	return (results);
}
