#include "main.h"
/**
 * _abs - Entry point
 * Description: 'return the absolute of n'
 * @num: character
 * Return: Always num.
 */
int _abs(int num)
{
	if (num < 0)
	{
		return (-num);
	}
	else
	{
		return (num);
	}
}
